using System.Collections;
using System.Collections.Generic;
using CharacterWeaponDataNamespace;
using UnityEngine;

public class CharacterWeaponClass : MonoBehaviour
{
    [SerializeField]
    private CharacterWeaponDataStruct characterWeaponDataStruct;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
